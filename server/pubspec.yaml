name: andfriend_park_server
description: Lightweight, privacy-preserving WebSocket relay server for And Friend's The Park community space.
version: 2.1.2
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
